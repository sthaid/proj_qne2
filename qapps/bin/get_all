#!/bin/bash

# cd to qapps dir
top=$(git rev-parse --show-toplevel)
cd $top/qapps

rm -rf files
android_exec "tar --exclude log -cf ../xfer.tar ."
android_exec "cat ../xfer.tar" > /tmp/xfer.tar
mkdir files
cd files
tar -xvf /tmp/xfer.tar
rm /tmp/xfer.tar

